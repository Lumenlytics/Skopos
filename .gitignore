# Generated human-readable export of the frame map; regenerate from the
# SavedVariables snapshot (see HANDOFF.md), don't version it.
FrameMap.xlsx

# Local secrets — the BigWigsMods packager auto-loads .env from the repo root or cwd
# (CF_API_KEY, WAGO_API_TOKEN, WOWI_API_TOKEN). Never commit it. Added fleet-wide
# 2026-08-15 after the TimeIsMoney chat found 8 of 9 repos had no guard.
.env
.env.*
!.env.example
